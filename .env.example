# Backend envs (do not expose to frontend)
OPENAI_API_KEY=
GEMINI_API_KEY=
STORAGE_BUCKET=
STORAGE_REGION=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
JWT_SECRET=

# Auth (DEV_TOKEN for local testing - use "Bearer dev-token" header)
DEV_TOKEN=dev-token

# Mercado Pago PIX (optional - uses mock mode if not configured)
MP_ACCESS_TOKEN=TEST-xxx-your-token
MP_WEBHOOK_SECRET=xxx
MERCADOPAGO_PUBLIC_KEY=TEST-xxx
PUBLIC_BASE_URL=https://your-domain.com
MP_NOTIFICATION_URL=https://your-domain.com/api/payments/webhook
