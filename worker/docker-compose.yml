services:
  worker:
    build: .
    ports:
      - "8000:8000"
    # Load secrets/config from ./worker/.env on the VM
    env_file:
      - .env
    environment:
      - WHISPER_MODEL=small
      - USE_GPU=false
    volumes:
      - ./tmp:/tmp/work
    restart: unless-stopped
